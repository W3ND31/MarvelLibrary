@model MarvelLibrary.Models.Fav

@{
    ViewData["Title"] = "Comics";
}


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewData["Title"]</title>
</head>
<body>

    <div class="mt-5">
        <h1 class="mt-5">Comics</h1>
        <p>
            <a asp-action="Comics" asp-route-id=@Model.CharacterId>Get Comics</a>
        </p>
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Id)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Id)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CharacterId)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CharacterId)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Index">Back to List</a>
    </div>
</body>
</html>
